/**
 * Public SDK subpath for command text normalization and routing decisions.
 */
export { normalizeCommandBody } from "../auto-reply/commands-registry-normalize.ts";
export { shouldHandleTextCommands } from "../auto-reply/commands-text-routing.ts";
